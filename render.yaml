
services:
  - type: web
    name: agendador
    env: python
    buildCommand: ""
    startCommand: "gunicorn src.main:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agendador-db
          property: connectionString

databases:
  - name: agendador-db
